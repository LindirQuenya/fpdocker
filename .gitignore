# Ignore my debug log directory.
debuglogs
# Ignore the local UnityTest files, as they're simply too big.
UnityTest/*.html
UnityTest/*.unity3d
# Don't ignore debs, we don't want to force everyone to rebuild wine.
#*.deb
